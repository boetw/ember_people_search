{{yield}}
<h2>{{title}}</h2>
<ul>
	{{#each people as |person|}}
		<li{{action "showPerson"  person.location.state}}><img src="{{person.picture.medium}}"><h2>{{person.name.first}} {{person.name.last}}</h2><h3>{{person.email}} <br>{{person.phone}}</h3></li>
	{{/each}}
</ul>